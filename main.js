'use strict';

/*const labels = document.querySelectorAll('label');
const backgrounds = [
	"trainingGirl1.jpg", 
	"trainingGirl2.jpg", 
	"trainingGirl3.jpg", 
	"trainingGirl4.jpg"
];
let preloadedImages = [];
for(let i = 0; i < backgrounds.length; ++i){
	preloadedImages[i] = new Image();
	preloadedImages[i].src = `images/${backgrounds[i]}`;
}

document.body.style.backgroundImage = `url(${preloadedImages[0].src})`;

labels.forEach((label, index) => {
	label.addEventListener('click', () => {
		document.body.className = '';
		document.body.className = `bg${index}`;
		document.body.style.backgroundImage = `url(${preloadedImages[index].src})`;
	});
});*/